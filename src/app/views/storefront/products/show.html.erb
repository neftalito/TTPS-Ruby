<div class="max-w-6xl mx-auto p-6">
  <div class="flex items-center justify-between mb-6">
    <div>
      <%= link_to storefront_products_path, class: "inline-flex items-center gap-2 px-3 py-2 bg-gray-100 text-gray-700 rounded-md hover:bg-gray-200" do %>
        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M7.707 14.707a1 1 0 01-1.414 0L2.586 11l3.707-3.707a1 1 0 011.414 1.414L5.414 11l2.293 2.293a1 1 0 010 1.414z" clip-rule="evenodd"/>
        </svg>
        <%= t("common.actions.back") %>
      <% end %>
    </div>

    <div class="text-right">
      <p class="text-sm uppercase tracking-wider text-gray-500"><%= t(".product_label") %></p>
      <h1 class="text-2xl font-semibold text-gray-800"><%= @product.name %></h1>
      <p class="text-base text-gray-600"><%= @product.author %></p>

      <% raw_type = @product.respond_to?(:product_type) ? @product.product_type : @product[:product_type] %>
      <% type_label = translated_product_type(raw_type) %>

      <% if type_label.present? %>
        <p class="mt-2">
          <span class="inline-block px-3 py-1 text-sm font-semibold bg-gray-100 text-gray-800 rounded">
            <%= type_label %>
          </span>
        </p>
      <% end %>
    </div>
  </div>

  <div class="flex flex-col md:flex-row gap-6">
    <div class="md:w-1/2">
      <div id="product-carousel" class="relative w-full" data-carousel="slide">
        <div class="relative h-96 overflow-hidden rounded-lg bg-gray-50">
          <% if @product.images.attached? %>
            <% @product.images.each_with_index do |img, index| %>
              <div class="hidden duration-700 ease-in-out <%= "active" if index == 0 %>" data-carousel-item>
                <div class="flex items-center justify-center h-full p-4">
                  <%= image_tag url_for(img), class: "max-h-full max-w-full object-contain" %>
                </div>
              </div>
            <% end %>
          <% else %>
            <div class="hidden duration-700 ease-in-out active" data-carousel-item>
              <div class="flex items-center justify-center h-full text-gray-500">
                <%= t("common.states.no_images") %>
              </div>
            </div>
          <% end %>
        </div>

        <% if @product.images.attached? && @product.images.count > 1 %>
          <button type="button" class="absolute top-0 start-0 z-30 flex items-center justify-center h-full px-4 cursor-pointer group focus:outline-none" data-carousel-prev>
            <span class="inline-flex items-center justify-center w-10 h-10 rounded-full bg-white/30 group-hover:bg-white/50 group-focus:ring-4 group-focus:ring-white group-focus:outline-none">
              <svg class="w-4 h-4 text-white rtl:rotate-180" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 6 10">
                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 1 1 5l4 4"/>
              </svg>
              <span class="sr-only"><%= t(".previous") %></span>
            </span>
          </button>
          <button type="button" class="absolute top-0 end-0 z-30 flex items-center justify-center h-full px-4 cursor-pointer group focus:outline-none" data-carousel-next>
            <span class="inline-flex items-center justify-center w-10 h-10 rounded-full bg-white/30 group-hover:bg-white/50 group-focus:ring-4 group-focus:ring-white group-focus:outline-none">
              <svg class="w-4 h-4 text-white rtl:rotate-180" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 6 10">
                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 9 4-4-4-4"/>
              </svg>
              <span class="sr-only"><%= t(".next") %></span>
            </span>
          </button>

          <div class="absolute z-30 flex -translate-x-1/2 bottom-5 left-1/2 space-x-3 rtl:space-x-reverse">
            <% @product.images.count.times do |i| %>
              <button type="button" class="w-3 h-3 rounded-full" aria-current="<%= i == 0 ? "true" : "false" %>" aria-label="<%= t(".slide", number: i + 1) %>" data-carousel-slide-to="<%= i %>"></button>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>

    <div class="md:w-1/2 space-y-6">
      <div class="grid grid-cols-2 gap-4">
        <div class="bg-gray-50 p-4 rounded">
          <p class="text-base font-semibold text-gray-800"><%= t("common.labels.genre") %></p>
          <p class="mt-1 text-lg font-medium text-gray-900"><%= @product.category&.name || "-" %></p>
        </div>

        <div class="bg-gray-50 p-4 rounded">
          <p class="text-base font-semibold text-gray-800"><%= t("common.labels.release_year") %></p>
          <p class="mt-1 text-lg font-medium text-gray-900">
            <%= @product.release_year || "-" %>
          </p>
        </div>

        <div class="bg-gray-50 p-4 rounded">
          <p class="text-base font-semibold text-gray-800"><%= t("common.labels.status") %></p>
          <p class="mt-1 text-lg font-medium text-gray-900"><%= translated_product_condition(@product.condition) %></p>
        </div>

        <div class="bg-gray-50 p-4 rounded">
          <p class="text-base font-semibold text-gray-800"><%= t("common.labels.price") %></p>
          <p class="mt-1 text-lg font-semibold text-indigo-600"><%= number_to_currency(@product.price || 0, unit: "$", separator: ",", delimiter: ".", format: "%u%n") %></p>
        </div>

        <div class="bg-gray-50 p-4 rounded col-span-2">
          <p class="text-base font-semibold text-gray-800"><%= t(".stock_available") %></p>
          <p class="mt-1 text-lg font-semibold <%= (@product.try(:stock) || 0).to_i > 0 ? "text-green-600" : "text-red-600" %>">
            <%= @product.try(:stock) || 0 %>
          </p>
        </div>
      </div>

      <div>
        <p class="text-base font-semibold text-gray-800"><%= t("common.labels.description") %></p>
        <div class="mt-2 prose max-w-none text-gray-800">
          <%= simple_format(@product.try(:description) || "") %>
        </div>
      </div>

      <% if @product.condition == "used" && @product.audio.attached? %>
        <div class="bg-gray-100 p-4 rounded mt-4">
          <p class="text-base font-semibold text-gray-800 mb-2"><%= t(".audio_sample") %></p>
          <audio controls class="w-full">
            <source src="<%= url_for(@product.audio) %>" type="<%= @product.audio.content_type %>">
            <%= t("common.messages.browser_no_audio_support") %>
          </audio>
        </div>
      <% end %>
    </div>
  </div>

  <% if @related_products.present? %>
    <div class="mt-16 border-t border-gray-200 pt-10">
      <h2 class="text-2xl font-bold text-gray-800 mb-6"><%= t(".related_products") %></h2>
      <hr class="my-6 border-gray-200">

      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-6">
        <% @related_products.each do |related| %>
          <%= render "storefront/products/card", product: related %>
        <% end %>
      </div>
    </div>
  <% end %>
</div>

<script>
  document.addEventListener("turbo:load", function() {
    if (typeof initFlowbite === "function" && document.querySelector('[data-carousel="slide"]')) {
      initFlowbite();
    }
  });
</script>
